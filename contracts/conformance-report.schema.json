{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sarmg.org/schemas/conformance-report-v1.json",
  "type": "object",
  "required": ["schema_version", "contract", "mode", "generated_at", "projects", "summary"],
  "properties": {
    "schema_version": { "const": 1 },
    "contract": { "const": "org.sarmg.http@1.0.0" },
    "mode": { "enum": ["inventory", "live"] },
    "generated_at": { "type": "string", "format": "date-time" },
    "projects": {
      "type": "array",
      "minItems": 6,
      "maxItems": 6,
      "contains": {
        "type": "object",
        "required": ["role"],
        "properties": { "role": { "const": "public_ingress" } }
      },
      "minContains": 1,
      "maxContains": 1,
      "items": {
        "type": "object",
        "required": ["id", "role", "checks", "summary"],
        "properties": {
          "id": { "type": "string" },
          "role": { "enum": ["public_ingress", "private_module"] },
          "checks": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["id", "category", "status", "message"],
              "properties": {
                "status": { "enum": ["pass", "fail", "waived", "not_run"] }
              }
            }
          }
        }
      }
    },
    "summary": { "type": "object" }
  }
}
